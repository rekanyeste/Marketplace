/// Storage repository - kept for API compatibility.
/// Images are now stored as Base64 in Firestore documents.
/// This file can be removed if no other code references it.
class StorageRepository {
  StorageRepository();
}
